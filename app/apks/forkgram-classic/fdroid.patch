diff --git a/TMessagesProj/build.gradle b/TMessagesProj/build.gradle
--- a/TMessagesProj/build.gradle
+++ b/TMessagesProj/build.gradle
@@ -60,12 +60,8 @@
     implementation 'com.stripe:stripe-android:2.0.2'
     implementation 'com.google.code.gson:gson:2.11.0'
     implementation 'com.google.guava:guava:31.1-android'
-    implementation 'com.google.android.play:integrity:1.3.0'
-    implementation 'com.google.android.gms:play-services-safetynet:18.0.1'
-
     implementation 'co.nstant.in:cbor:0.8'
     implementation "androidx.credentials:credentials:1.6.0-alpha04"
-    implementation "androidx.credentials:credentials-play-services-auth:1.6.0-alpha04"
 
     implementation "io.noties.markwon:core:4.6.2"
     implementation "io.noties.markwon:ext-strikethrough:4.6.2"
@@ -99,8 +95,6 @@
     buildToolsVersion '36.0.0'
     ndkVersion "27.2.12479018"
 
-    sourceSets.main.jniLibs.srcDirs = ['./jni/']
-
     externalNativeBuild {
         cmake {
             path 'jni/CMakeLists.txt'
@@ -118,14 +112,14 @@
     }
 
     compileOptions {
-        sourceCompatibility JavaVersion.VERSION_17
-        targetCompatibility JavaVersion.VERSION_17
+        sourceCompatibility JavaVersion.VERSION_21
+        targetCompatibility JavaVersion.VERSION_21
 
         coreLibraryDesugaringEnabled true
     }
 
     kotlinOptions {
-        jvmTarget = JavaVersion.VERSION_17
+        jvmTarget = JavaVersion.VERSION_21
     }
 
     defaultConfig.versionCode = Integer.parseInt(Utils['getVersionCode']())
diff --git a/TMessagesProj_App/build.gradle b/TMessagesProj_App/build.gradle
--- a/TMessagesProj_App/build.gradle
+++ b/TMessagesProj_App/build.gradle
@@ -52,8 +52,6 @@
 
     defaultConfig.applicationId = "org.forkgram.classic"
 
-    sourceSets.main.jniLibs.srcDirs = ['../TMessagesProj/jni/']
-
     lintOptions {
         disable 'MissingTranslation'
         disable 'ExtraTranslation'
@@ -61,8 +59,8 @@
     }
 
     compileOptions {
-        sourceCompatibility JavaVersion.VERSION_17
-        targetCompatibility JavaVersion.VERSION_17
+        sourceCompatibility JavaVersion.VERSION_21
+        targetCompatibility JavaVersion.VERSION_21
 
         coreLibraryDesugaringEnabled true
     }
@@ -75,7 +73,21 @@
     }
 
     signingConfigs {
-        if (!fdroid) {
+        if (fdroid) {
+            debug {
+                storeFile file("../TMessagesProj/config/release.keystore")
+                storePassword "android"
+                keyAlias "androidkey"
+                keyPassword "android"
+            }
+
+            release {
+                storeFile file("../TMessagesProj/config/release.keystore")
+                storePassword "android"
+                keyAlias "androidkey"
+                keyPassword "android"
+            }
+        } else {
             debug {
                 storeFile file("../TMessagesProj/config/release.keystore")
                 storePassword RELEASE_STORE_PASSWORD
@@ -90,9 +102,6 @@
                 keyPassword RELEASE_KEY_PASSWORD
             }
         }
-        // When fdroid=true: no signingConfigs declared. AGP auto-signs debug
-        // variants with the local debug.keystore; release variants are left
-        // unsigned, which is what the F-Droid build server expects.
     }
 
     buildTypes {
@@ -131,7 +140,7 @@
         release {
             debuggable false
             jniDebuggable false
-            if (!fdroid) signingConfig signingConfigs.release
+            signingConfig signingConfigs.release
             applicationIdSuffix appSuffix
             minifyEnabled true
             shrinkResources false
