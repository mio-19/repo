diff --git a/app/src/full/kotlin/org/fdroid/ui/nearby/SwapSuccessModel.kt b/app/src/full/kotlin/org/fdroid/ui/nearby/SwapSuccessModel.kt
index 1696437..9ff7b28 100644
--- a/app/src/full/kotlin/org/fdroid/ui/nearby/SwapSuccessModel.kt
+++ b/app/src/full/kotlin/org/fdroid/ui/nearby/SwapSuccessModel.kt
@@ -22,5 +22,5 @@ data class SwapSuccessItem(
     get() = installedVersionCode != null && installedVersionCode >= versionCode
 
   val hasUpdate: Boolean
-    get() = installedVersionCode != null && installedVersionCode < versionCode
+    get() = installedVersionCode != null && (installedVersionCode < versionCode || (installedVersionCode == versionCode && installedVersionName != null && installedVersionName != versionName))
 }
diff --git a/app/src/main/kotlin/org/fdroid/ui/details/AppDetailsItem.kt b/app/src/main/kotlin/org/fdroid/ui/details/AppDetailsItem.kt
index 98b7e53..d1ae54b 100644
--- a/app/src/main/kotlin/org/fdroid/ui/details/AppDetailsItem.kt
+++ b/app/src/main/kotlin/org/fdroid/ui/details/AppDetailsItem.kt
@@ -194,7 +194,10 @@ data class AppDetailsItem(
           MainButtonState.LOADING
         } else if (
           suggestedVersion == null ||
-            suggestedVersion.versionCode <= installedVersionCode ||
+            (suggestedVersion.versionCode < installedVersionCode) ||
+            (suggestedVersion.versionCode == installedVersionCode &&
+              (installedVersionName == null ||
+                suggestedVersion.versionName == installedVersionName)) ||
             suggestedVersion.versionCode <= (appPrefs?.ignoreVersionCodeUpdate ?: 0)
         ) {
           MainButtonState.NONE
diff --git a/app/src/main/kotlin/org/fdroid/ui/details/DetailsPresenter.kt b/app/src/main/kotlin/org/fdroid/ui/details/DetailsPresenter.kt
index 1e713da..0e69767 100644
--- a/app/src/main/kotlin/org/fdroid/ui/details/DetailsPresenter.kt
+++ b/app/src/main/kotlin/org/fdroid/ui/details/DetailsPresenter.kt
@@ -219,7 +219,10 @@ fun DetailsPresenter(
           if (
             installedVersionCode == null ||
               possibleUpdate == null ||
-              possibleUpdate.versionCode <= installedVersionCode
+              (possibleUpdate.versionCode < installedVersionCode ||
+                (possibleUpdate.versionCode == installedVersionCode &&
+                  (packageInfo?.versionName == null ||
+                    possibleUpdate.versionName == packageInfo.versionName)))
           ) {
             null
           } else {
@@ -257,7 +260,11 @@ fun DetailsPresenter(
             if (!signerCompatible || installState.showProgress) {
               false
             } else {
-              (installedVersionCode ?: 0) < version.versionCode
+              val installedCode = installedVersionCode ?: 0
+              installedCode < version.versionCode ||
+                (installedCode == version.versionCode &&
+                  packageInfo?.versionName != null &&
+                  packageInfo.versionName != version.versionName)
             },
         )
       },
diff --git a/libs/database/src/main/java/org/fdroid/database/DbAppChecker.kt b/libs/database/src/main/java/org/fdroid/database/DbAppChecker.kt
index f73eb1b..4ada74d 100644
--- a/libs/database/src/main/java/org/fdroid/database/DbAppChecker.kt
+++ b/libs/database/src/main/java/org/fdroid/database/DbAppChecker.kt
@@ -68,6 +68,7 @@ public class DbAppChecker(
             versions = versions,
             allowedSignersGetter = null, // all signers are allowed
             installedVersionCode = getLongVersionCode(packageInfo),
+            installedVersionName = packageInfo.versionName,
             allowedReleaseChannels = null,
             includeKnownVulnerabilities = true,
             preferencesGetter = { appPrefsDao.getAppPrefsOrNull(packageName) },
diff --git a/libs/index/src/androidMain/kotlin/org/fdroid/UpdateChecker.kt b/libs/index/src/androidMain/kotlin/org/fdroid/UpdateChecker.kt
index 22d9b8d..0a96af5 100644
--- a/libs/index/src/androidMain/kotlin/org/fdroid/UpdateChecker.kt
+++ b/libs/index/src/androidMain/kotlin/org/fdroid/UpdateChecker.kt
@@ -40,6 +40,7 @@ public class UpdateChecker(private val compatibilityChecker: CompatibilityChecke
         packageInfo.signatures?.map { getPackageSigner(it.toByteArray()) }?.toSet()
       },
       installedVersionCode = PackageInfoCompat.getLongVersionCode(packageInfo),
+      installedVersionName = packageInfo.versionName,
       allowedReleaseChannels = releaseChannels,
       preferencesGetter = preferencesGetter,
       includeKnownVulnerabilities = includeKnownVulnerabilities,
@@ -89,6 +90,7 @@ public class UpdateChecker(private val compatibilityChecker: CompatibilityChecke
     versions: List<T>,
     allowedSignersGetter: (() -> Set<String>?)? = null,
     installedVersionCode: Long = 0,
+    installedVersionName: String? = null,
     allowedReleaseChannels: List<String>? = null,
     includeKnownVulnerabilities: Boolean = false,
     preferencesGetter: (() -> PackagePreference?)? = null,
@@ -97,6 +99,7 @@ public class UpdateChecker(private val compatibilityChecker: CompatibilityChecke
         versions = versions,
         allowedSignersGetter = allowedSignersGetter,
         installedVersionCode = installedVersionCode,
+        installedVersionName = installedVersionName,
         allowedReleaseChannels = allowedReleaseChannels,
         includeKnownVulnerabilities = includeKnownVulnerabilities,
         preferencesGetter = preferencesGetter,
@@ -113,6 +116,7 @@ public class UpdateChecker(private val compatibilityChecker: CompatibilityChecke
     versions: List<T>,
     allowedSignersGetter: (() -> Set<String>?)? = null,
     installedVersionCode: Long = 0,
+    installedVersionName: String? = null,
     allowedReleaseChannels: List<String>? = null,
     includeKnownVulnerabilities: Boolean = false,
     preferencesGetter: (() -> PackagePreference?)? = null,
@@ -128,7 +132,10 @@ public class UpdateChecker(private val compatibilityChecker: CompatibilityChecke
       )
         yield(version)
       // if version code is not higher than installed skip package as list is sorted
-      if (version.versionCode <= installedVersionCode) return@sequence
+      if (version.versionCode < installedVersionCode) return@sequence
+      if (version.versionCode == installedVersionCode &&
+        (installedVersionName == null || version.versionName == installedVersionName)
+      ) return@versions
       // we don't support versions that have multiple signers
       if (version.signer?.hasMultipleSigners == true) return@versions
       // skip incompatible versions
