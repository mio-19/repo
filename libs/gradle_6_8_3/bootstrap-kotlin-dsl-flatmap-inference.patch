diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/AccessorsClassPath.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/AccessorsClassPath.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/AccessorsClassPath.kt	2026-05-01 07:02:03.845987919 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/AccessorsClassPath.kt	2026-05-01 07:02:03.897987430 +1000
@@ -240,7 +240,8 @@
 internal
 fun defaultPackageTypesIn(typeStrings: List<String>): List<String> =
     typeStrings
-        .flatMap { classNamesFromTypeString(it).all }
+        .map { classNamesFromTypeString(it).all }
+        .flatten()
         .filter { '.' !in it }
         .distinct()

@@ -300,7 +301,7 @@

     private
     fun inaccessibilityReasonsFor(classNames: ClassNamesFromTypeString): List<InaccessibilityReason> =
-        classNames.all.flatMap { inaccessibilityReasonsFor(it) }.let { inaccessibilityReasons ->
+        classNames.all.map { inaccessibilityReasonsFor(it) }.flatten().let { inaccessibilityReasons ->
             if (inaccessibilityReasons.isNotEmpty()) inaccessibilityReasons
             else classNames.leaves.filter(::hasTypeParameter).map(::typeErasure)
         }
diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/PluginAccessorsClassPath.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/PluginAccessorsClassPath.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/PluginAccessorsClassPath.kt	2026-05-01 07:02:03.851987862 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/PluginAccessorsClassPath.kt	2026-05-01 07:02:03.899987410 +1000
@@ -503,7 +503,8 @@
         .asFiles
         .asSequence()
         .filter { it.isFile && it.extension.equals("jar", true) }
-        .flatMap { pluginEntriesFrom(it).asSequence() }
+        .map { pluginEntriesFrom(it).asSequence() }
+        .flatten()
         .map { PluginTree.PluginSpec(it.pluginId, it.implementationClass) }


diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/tasks/PrintAccessors.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/tasks/PrintAccessors.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/tasks/PrintAccessors.kt	2026-05-01 07:02:03.859987787 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/accessors/tasks/PrintAccessors.kt	2026-05-01 07:02:03.901987391 +1000
@@ -64,9 +64,9 @@

 private
 fun accessorSourceFragmentsFor(schema: TypedProjectSchema): Sequence<String> =
-    accessorsFor(schema.map(::accessible)).flatMap { accessor ->
+    accessorsFor(schema.map(::accessible)).map { accessor ->
         val (_, fragments) = fragmentsFor(accessor)
         fragments
             .map { it.source }
             .filter { it.isNotBlank() }
-    }
+    }.flatten()
diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/ApiExtensionsGenerator.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/ApiExtensionsGenerator.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/ApiExtensionsGenerator.kt	2026-05-01 07:02:03.867987711 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/ApiExtensionsGenerator.kt	2026-05-01 07:02:03.903987373 +1000
@@ -104,7 +104,8 @@
             val relativeSourcePath = relativeSourcePathOf(type)
             type.isPublic && apiSpec.test(relativeSourcePath.segments, relativeSourcePath.isFile)
         }
-        .flatMap { type -> kotlinExtensionFunctionsFor(type) }
+        .map { type -> kotlinExtensionFunctionsFor(type) }
+        .flatten()
         .distinctBy(::signatureKey)


@@ -116,15 +117,15 @@
 private
 fun signatureKey(extension: KotlinExtensionFunction): List<Any> = extension.run {
     listOf(targetType.sourceName, name) +
-        parameters.flatMap { apiTypeKey(it.type) }
+        parameters.map { apiTypeKey(it.type) }.flatten()
 }


 private
 fun apiTypeKey(usage: ApiTypeUsage): List<Any> = usage.run {
     listOf(sourceName, isNullable, isRaw, variance) +
-        typeArguments.flatMap(::apiTypeKey) +
-        bounds.flatMap(::apiTypeKey)
+        typeArguments.map(::apiTypeKey).flatten() +
+        bounds.map(::apiTypeKey).flatten()
 }


@@ -143,7 +144,7 @@
 fun kotlinExtensionFunctionsFor(type: ApiType): Sequence<KotlinExtensionFunction> =
     candidatesForExtensionFrom(type)
         .sortedWithTypeOfTakingFunctionsFirst()
-        .flatMap { function ->
+        .map { function ->

             val candidateFor = object {

@@ -158,7 +159,7 @@
             }

             if (!candidateFor.extension) {
-                return@flatMap emptySequence<KotlinExtensionFunction>()
+                return@map emptySequence<KotlinExtensionFunction>()
             }

             val extensionTypeParameters = function.typeParameters + type.typeParameters
@@ -176,6 +177,7 @@
                 )
             )
         }
+        .flatten()


 private
diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/PluginIdExtensions.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/PluginIdExtensions.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/PluginIdExtensions.kt	2026-05-01 07:02:03.873987655 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/codegen/PluginIdExtensions.kt	2026-05-01 07:02:03.903987373 +1000
@@ -78,7 +78,8 @@
     jars
         .asSequence()
         .filter { it.name.startsWith("gradle-") }
-        .flatMap(::pluginExtensionsFrom)
+        .map { file -> pluginExtensionsFrom(file) }
+        .flatten()


 private
diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/FindGradleSources.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/FindGradleSources.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/FindGradleSources.kt	2026-05-01 07:02:03.881987579 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/FindGradleSources.kt	2026-05-01 07:02:03.905987354 +1000
@@ -44,9 +44,9 @@
     private
     fun registerSourceDirectories(outputs: TransformOutputs) {
         unzippedSubProjectsDir()?.let {
-            subDirsOf(it).flatMap { subProject ->
+            subDirsOf(it).map { subProject ->
                 subDirsOf(File(subProject, "src/main"))
-            }.forEach { outputs.dir(it) }
+            }.flatten().forEach { outputs.dir(it) }
         }
     }

diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/SourceDistributionProvider.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/SourceDistributionProvider.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/SourceDistributionProvider.kt	2026-05-01 07:02:03.887987523 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/resolver/SourceDistributionProvider.kt	2026-05-01 07:02:03.907987334 +1000
@@ -61,14 +61,14 @@

     private
     fun registerTransforms() {
-        registerTransform<UnzipDistribution> {
-            from.attribute(artifactType, zipType)
-            to.attribute(artifactType, unzippedDistributionType)
-        }
-        registerTransform<FindGradleSources> {
-            from.attribute(artifactType, unzippedDistributionType)
-            to.attribute(artifactType, sourceDirectory)
-        }
+        registerTransform<UnzipDistribution>(Action<TransformSpec<TransformParameters.None>> {
+            from.attribute(artifactType, zipType)
+            to.attribute(artifactType, unzippedDistributionType)
+        })
+        registerTransform<FindGradleSources>(Action<TransformSpec<TransformParameters.None>> {
+            from.attribute(artifactType, unzippedDistributionType)
+            to.attribute(artifactType, sourceDirectory)
+        })
     }

     private
@@ -92,7 +92,7 @@
     )

     private
-    fun createSourceRepository() = ivy {
+    fun createSourceRepository() = ivy(Action<IvyArtifactRepository> {
         val repoName = repositoryNameFor(gradleVersion)
         name = "Gradle $repoName"
         setUrl("https://services.gradle.org/$repoName")
@@ -105,7 +105,7 @@
             }
             artifact("[module]-[revision](-[classifier])(.[ext])")
         }
-    }
+    })

     private
     fun isSnapshot(gradleVersion: String) = gradleVersion.contains('+')
diff -ruN a/subprojects/kotlin-dsl-provider-plugins/src/main/kotlin/org/gradle/kotlin/dsl/provider/plugins/precompiled/tasks/GeneratePrecompiledScriptPluginAccessors.kt b/subprojects/kotlin-dsl-provider-plugins/src/main/kotlin/org/gradle/kotlin/dsl/provider/plugins/precompiled/tasks/GeneratePrecompiledScriptPluginAccessors.kt
--- a/subprojects/kotlin-dsl-provider-plugins/src/main/kotlin/org/gradle/kotlin/dsl/provider/plugins/precompiled/tasks/GeneratePrecompiledScriptPluginAccessors.kt	2026-05-01 07:16:00.000000000 +1000
+++ b/subprojects/kotlin-dsl-provider-plugins/src/main/kotlin/org/gradle/kotlin/dsl/provider/plugins/precompiled/tasks/GeneratePrecompiledScriptPluginAccessors.kt	2026-05-01 07:16:00.000000000 +1000
@@ -291,7 +291,7 @@
             rootProjectClassPath = (classPathFiles + runtimeClassPathFiles).files,
             projectSchemaProvider = projectSchemaProvider
         ).useToRun {
-            pluginGroupsPerRequests.flatMap { (uniquePluginRequests, scriptPlugins) ->
+            pluginGroupsPerRequests.map { (uniquePluginRequests, scriptPlugins) ->
                 try {
                     val schema = schemaFor(pluginRequestsFor(uniquePluginRequests, scriptPlugins.first()))
                     val hashedSchema = HashedProjectSchema(schema)
@@ -300,7 +300,7 @@
                     reportProjectSchemaError(scriptPlugins, error)
                     emptyList<Pair<HashedProjectSchema, PrecompiledScriptPlugin>>()
                 }
-            }.groupBy(
+            }.flatten().groupBy(
                 { (schema, _) -> schema },
                 { (_, plugin) -> plugin }
             )
diff -ruN a/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/BuildSrcClassPathModeConfigurationAction.kt b/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/BuildSrcClassPathModeConfigurationAction.kt
--- a/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/BuildSrcClassPathModeConfigurationAction.kt	2026-05-01 09:07:00.000000000 +1000
+++ b/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/BuildSrcClassPathModeConfigurationAction.kt	2026-05-01 09:07:00.000000000 +1000
@@ -93,7 +93,11 @@

     private
     fun Sequence<Project>.allSourceSetsRoots() =
-        flatMap { it.sourceSets.flatMap { it.allSource.srcDirs }.asSequence() }
+        map { project ->
+            project.sourceSets
+                .map { sourceSet -> sourceSet.allSource.srcDirs }
+                .flatten().asSequence()
+        }.flatten()

     private
     val Project.sourceSets
diff -ruN a/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinBuildScriptTemplateModelBuilder.kt b/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinBuildScriptTemplateModelBuilder.kt
--- a/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinBuildScriptTemplateModelBuilder.kt	2026-05-01 09:07:00.000000000 +1000
+++ b/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinBuildScriptTemplateModelBuilder.kt	2026-05-01 09:07:00.000000000 +1000
@@ -42,7 +42,8 @@
             StandardKotlinBuildScriptTemplateModel(
                 gradleModules
                     .map { getModule(it) }
-                    .flatMap { it.allRequiredModules }
+                    .map { it.allRequiredModules }
+                    .flatten()
                     .fold(ClassPath.EMPTY) { classPath, module -> classPath + module.classpath }
                     .asFiles
             )
diff -ruN a/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinLibSources.kt b/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinLibSources.kt
--- a/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinLibSources.kt	2026-05-01 09:07:00.000000000 +1000
+++ b/subprojects/kotlin-dsl-tooling-builders/src/main/kotlin/org/gradle/kotlin/dsl/tooling/builders/KotlinLibSources.kt	2026-05-01 09:07:00.000000000 +1000
@@ -107,7 +107,8 @@
             .withArtifacts(JvmLibrary::class.java, SourcesArtifact::class.java)
             .execute()
             .resolvedComponents
-            .flatMap { it.getArtifacts(SourcesArtifact::class.java) }
+            .map { component -> component.getArtifacts(SourcesArtifact::class.java) }
+            .flatten()
             .filterIsInstance<ResolvedArtifactResult>()
             .map { it.file }
             .sorted()
diff -ruN a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/support/ClassBytesRepository.kt b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/support/ClassBytesRepository.kt
--- a/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/support/ClassBytesRepository.kt	2026-05-01 07:02:03.895987447 +1000
+++ b/subprojects/kotlin-dsl/src/main/kotlin/org/gradle/kotlin/dsl/support/ClassBytesRepository.kt	2026-05-01 07:02:03.909987317 +1000
@@ -71,7 +71,8 @@
      */
     fun allClassesBytesBySourceName(): Sequence<Pair<String, ClassBytesSupplier>> =
         classPathFiles.asSequence()
-            .flatMap { sourceNamesFrom(it) }
+            .map { sourceNamesFrom(it) }
+            .flatten()
             .mapNotNull { sourceName ->
                 classBytesSupplierForSourceName(sourceName)?.let { sourceName to it }
             }
@@ -185,7 +186,8 @@
 private
 fun nestedClassFilePathCandidatesFor(path: String): Sequence<String> =
     generateSequence({ nestedClassNameFor(path) }, ::nestedClassNameFor)
-        .flatMap(::candidateClassFiles)
+        .map(::candidateClassFiles)
+        .flatten()


 private
