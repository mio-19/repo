--- a/services/core/java/com/android/server/adb/AdbDebuggingManager.java
+++ b/services/core/java/com/android/server/adb/AdbDebuggingManager.java
@@ -813,22 +813,19 @@
                     }
 
                     AdbConnectionInfo currentInfo = getCurrentWifiApInfo();
-                    if (currentInfo == null) {
-                        Settings.Global.putInt(
-                                mContentResolver, Settings.Global.ADB_WIFI_ENABLED, 0);
-                        break;
-                    }
-
-                    if (!verifyWifiNetwork(currentInfo.getBSSID(), currentInfo.getSSID())) {
-                        // This means that the network is not in the list of trusted networks.
-                        // We'll give user a prompt on whether to allow wireless debugging on
-                        // the current wifi network.
-                        Settings.Global.putInt(
-                                mContentResolver, Settings.Global.ADB_WIFI_ENABLED, 0);
-                        break;
+                    if (currentInfo != null) {
+                        if (!verifyWifiNetwork(currentInfo.getBSSID(), currentInfo.getSSID())) {
+                            // This means that the network is not in the list of trusted networks.
+                            // We'll give user a prompt on whether to allow wireless debugging on
+                            // the current wifi network.
+                            Settings.Global.putInt(
+                                    mContentResolver, Settings.Global.ADB_WIFI_ENABLED, 0);
+                            break;
+                        }
+                        mAdbConnectionInfo.copy(currentInfo);
+                    } else {
+                        mAdbConnectionInfo.clear();
                     }
-
-                    mAdbConnectionInfo.copy(currentInfo);
                     mAdbNetworkMonitor.register();
                     ensureAdbDebuggingThreadAlive();
                     startTLSPortPoller();
@@ -1091,7 +1088,7 @@
             WifiManager wifiManager = (WifiManager) mContext.getSystemService(Context.WIFI_SERVICE);
             WifiInfo wifiInfo = wifiManager.getConnectionInfo();
             if (wifiInfo == null || wifiInfo.getNetworkId() == -1) {
-                Slog.i(TAG, "Not connected to any wireless network. Not enabling adbwifi.");
+                Slog.i(TAG, "No current Wi-Fi network information available for adbwifi.");
                 return null;
             }
 

diff --git a/services/core/java/com/android/server/adb/AdbBroadcastReceiver.java b/services/core/java/com/android/server/adb/AdbBroadcastReceiver.java
index 7949080..12a6dda 100644
--- a/services/core/java/com/android/server/adb/AdbBroadcastReceiver.java
+++ b/services/core/java/com/android/server/adb/AdbBroadcastReceiver.java
@@ -73,6 +73,10 @@ public class AdbBroadcastReceiver extends BroadcastReceiver implements AdbNetwor
         Settings.Global.putInt(mContentResolver, Settings.Global.ADB_WIFI_ENABLED, 0);
     }
 
+    private boolean shouldIgnoreMissingWifiContext() {
+        return TextUtils.isEmpty(mAdbConnectionInfo.getBSSID());
+    }
+
     @Override
     public void onReceive(Context context, Intent intent) {
         String action = intent.getAction();
@@ -84,9 +88,18 @@ public class AdbBroadcastReceiver extends BroadcastReceiver implements AdbNetwor
                             WifiManager.EXTRA_WIFI_STATE, WifiManager.WIFI_STATE_DISABLED);
 
             if (state == WifiManager.WIFI_STATE_DISABLED) {
+                if (shouldIgnoreMissingWifiContext()) {
+                    Slog.i(TAG, "Wifi disabled with no remembered AP context. Keeping adbwifi.");
+                    return;
+                }
                 disableWifi("Wifi disabled. Disabling adbwifi.");
             }
         } else if (WifiManager.NETWORK_STATE_CHANGED_ACTION.equals(action)) {
+            if (shouldIgnoreMissingWifiContext()) {
+                Slog.i(TAG, "Network change with no remembered AP context. Keeping adbwifi.");
+                return;
+            }
+
             // We only care about wifi type connections
             NetworkInfo networkInfo =
                     (NetworkInfo)
